{
  "name": "NexSplit",
  "version": "1.0.0",
  "description": "NexSplit",
  "type": "module",
  "keywords": [
    "capacitor",
    "mobile"
  ],
  "scripts": {
    "build:init": "npx cap add android && npx cap add ios",
    "build:all": "npx cap copy && npx cap sync",
    "build:android": "npx cap copy && npx cap sync android",
    "build:ios": "npx cap copy && npx cap sync ios"
  },
  "dependencies": {
    "@capacitor/android": "^7.4.2",
    "@capacitor/camera": "latest",
    "@capacitor/core": "^7.4.2",
    "@capacitor/ios": "^7.4.2",
    "@capacitor/splash-screen": "latest"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.4.2"
  },
  "author": "NexSplit",
  "license": "ISC"
}
